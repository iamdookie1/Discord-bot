flask>=3.0.0
discord.py>=2.3.2

# Pre-installed for custom commands (see bot_commands.py) so they're
# available immediately without a separate install step. Picked to be
# pure-Python / build-free so they install reliably on Termux.
requests>=2.31.0
pytz>=2024.1
python-dateutil>=2.9.0
humanize>=4.9.0
emoji>=2.11.0
beautifulsoup4>=4.12.0
PyYAML>=6.0.1
colorama>=0.4.6
tabulate>=0.9.0
validators>=0.28.0
